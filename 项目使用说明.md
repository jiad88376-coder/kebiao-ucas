# 课壳 · 项目使用说明

> 「课壳」— 国科大人自己的课表工具。粘贴课程代码，3 秒生成整学期课表。
>
> 本文档随版本迭代更新：每次发版（sw.js `CACHE` 升版）时，同步更新文末「版本记录」。
>
> 文档版本：v41 · 更新日期：2026-09-12

## 在线入口

| 入口 | 地址 | 说明 |
|---|---|---|
| 主站（国内直连） | https://kebiao-ucas.netlify.app | Netlify，国内可直接访问 |
| 备用 | https://jiad88376-coder.github.io/kebiao-ucas/ | GitHub Pages |
| 宣传域名 | https://kebiao.courseshell.cloud | 301 跳转到备用入口 |
| 运行监测 | （主站地址）/dashboard.html | 日活 / 论坛 / 赞赏漏斗 |

---

## 一、快速上手（用户视角）

### 1. 生成课表
- 粘贴课程代码：在选课系统里复制课程代码（如 `180081070200P1001H-1`），每行一个，点「生成课表」。
- 搜索添加：输入课程名称或代码，从建议列表点选。
- 支持批量：`更多 → 粘贴课程代码` 一次导入全班同学的代码（Ctrl+Enter 也能提交）。
- 时间冲突自动检测：精确到「周次 × 星期 × 节次」，补考周次不重叠不会误报；冲突的课块有红框和「冲突」角标。

### 2. 看课表
- 周切换：顶栏 ‹ › 翻周，点周标签回到本周，「全部周次」看整学期。
- 手机端默认聚焦今天（单日大视图），星期条一键切换；电脑端默认整周网格。
- 本周无课时自动顺延到最近有课的周，并提示。
- 单日视图：☀️上午 / 🌤下午 / 🌙晚上 三段卡片，置顶逐小时天气（按课程所在校区取坐标），空课时段有彩蛋文案。
- 课程色块按属性着色（核心/专业/公共/研讨/实验），底部有图例。

### 3. 课程详情（点课程块打开抽屉）
- 基本信息卡：教师 / 院系 / 学分 / 学时 / 考核方式 / 上课时间与教室。
- 笔记：可带图（本地压缩，最多 6 张），按日期归档。
- 作业：截止日倒计时徽章（剩 N 天 / 已逾期）。
- 考试：类型 / 日期 / 时间 / 地点，倒计时。
- 📍 调整信息：只改你自己课表里的显示——换教室、换老师、换上课时间（星期/节次），可选「整个学期」或「仅某一天」（调课临时安排）。会跟随云同步。
- 🔔 上课提醒：生成 .ics 日历文件 → 用系统「日历」打开 → 全学期课次连同提醒一次性写入，由系统按时提醒（iOS/安卓/电脑通用）。可选范围（仅下一节 / 全学期）和提前量（5/10/15/30 分钟）。
- 💬 讨论区：这门课的讨论与资料共享区。

### 4. 云同步与备份
- 未登录：一切数据只存在本机浏览器 localStorage。
- 登录（邮箱 + 密码，注册需邮箱验证码）：课表 / 笔记 / 作业 / 考试自动云同步；换设备登录同一账号即可互通。双端都有数据时按「修改时间新者胜」静默合并。
- `更多 → 立即云备份`：手动推送一次。
- `更多 → 备份与恢复`：导出/导入 JSON 备份文件（清缓存、换手机前的保险）。

### 5. 安装与桌面直达
- 安装成 App：`更多 → 安装成手机 App`（安卓浏览器菜单「安装应用」；iPhone 用 Safari 分享 → 添加到主屏幕）。安装后离线也能看课表。
- 桌面快捷方式（Android）：安装后**长按课壳图标** → 「今日课程」「下节课」直达。
- iPhone/iPad：iOS 不支持网页的快捷方式长按菜单，用「快捷指令」App 建「打开 URL」指向 `https://jiad88376-coder.github.io/kebiao-ucas/?view=next`，再添加到主屏幕即可（`更多 → 桌面快捷方式` 里有分步图文）。
- 应用角标：今天还剩几节课（Chromium 系支持，iOS 静默跳过）。

### 6. 分享与社区
- `分享`：推荐给同学，带你的编号（你是第 N 位课壳人）；支持 `?c=代码1,代码2` 链接一键导入。
- 自由论坛：发帖 / 回帖 / 📎上传资料（≤5MB）。未登录可浏览，发言需登录；只能删除自己的内容。
- 每门课有独立的「讨论与资料区」（课程抽屉 → 讨论区）。
- 感谢名单：页脚「🙏 感谢名单」；赞赏走页脚「☕ 请作者喝杯奶茶」。

### 7. 外观
- 🌗 主题按钮：跟随系统 → 强制深色 → 强制浅色 三档循环，系统主题变化时实时跟随。

### 8. 消息提醒（手机推送）
- `更多 → 消息提醒（手机推送）`：开启后由课壳直接推送——每天 7:30 今日课程（有课才发）、20:00 明日作业/考试截止、周日 19:00 下周课表预览；三类可分别开关。
- 与「系统日历提醒」相互独立：无需导入日历，内容随课表变化（含你的调课/换教室）；iOS 16.4+ 需先把课壳「添加到主屏幕」再从桌面打开；国内部分安卓浏览器收不到（依赖 Google 推送服务），可继续用日历提醒。
- 提醒任务只提前算 30 天，打开 App 会自动续算；换设备需重新开启。

---

## 二、兼容性说明

| 环境 | 情况 |
|---|---|
| iOS Safari | 可添加到主屏幕；无长按快捷方式、无应用角标；提醒走系统日历 |
| Android Chrome | 完整 PWA（安装、快捷方式、角标） |
| 微信 / QQ 内置 | 无法安装 PWA，顶部有引导条提示「右上角 ⋯ → 在浏览器打开」 |
| 离线 | 已缓存页面与课程库可离线查看；论坛 / 同步 / 天气需网络 |

---

## 三、维护者指南

### 1. 目录结构
```
schedule-app/
├── index.html / app.js / style.css / manifest.json / sw.js   # PWA 外壳与逻辑（无构建、原生 JS）
├── package.json                       # 唯一依赖 web-push（Netlify 定时函数用）
├── dashboard.html                     # 运行监测板（独立页，直连统计 RPC）
├── vendor/supabase.min.js             # supabase-js 本地化（离线可用）
├── data/schools.json                  # 学校注册表（id/名称/别名/默认校）
├── data/schools/<id>.json             # 校级配置（作息/校历/校区坐标/分段）
├── data/schools/<id>-catalog.json     # 课程库（脚本生成，勿手改）
├── netlify/functions/supabase.js      # Supabase 反代（备线路，含 /diag 诊断）
├── netlify/functions/push-sender.mjs  # 消息提醒定时投递（cron，每 10 分钟扫到期任务）
├── functions/cloudflare/worker.js     # Supabase 反代（主线路，绑定 api.courseshell.cloud）
├── functions/api/supabase/[[path]].js # Supabase 反代（EdgeOne 备胎，站点已回收 · 保留待恢复）
├── sql/                               # user_data / forum / push / 支持计数 建表脚本
├── scripts/                           # 课程库生成脚本（Python + openpyxl）
├── test/test.js                       # 纯逻辑单元测试（node 直接跑）
├── support-qr.jpg                     # 「请作者喝杯奶茶」页收款码
└── icons/                             # PWA 图标
```

### 2. 本地预览 / 测试
```bash
cd schedule-app
python -m http.server 8080     # 打开 http://localhost:8080
node test/test.js              # 94 项纯逻辑单测（解析/冲突/周次/微调/ICS/下节课/提醒任务/工具函数）
```
app.js 尾部 `module.exports` 暴露可测函数；DOM 相关逻辑不进测试。

### 3. 更新课程库（每学期）
1. 把教务导出的 xlsx 放本地，修改对应脚本头部的 `SRC` 路径：
   - `scripts/export_catalog.py` — 校本部（24 列版式）
   - `scripts/export_catalog_hias.py` — 杭高院
   - `scripts/export_catalog_qingdao.py` — 海洋学院（青岛）
   - `scripts/rebuild_ucas_from_export.py` — 以最新导出**重建**校本部库（新数据覆盖 + 保留现库独有课程）
   - `scripts/merge_kuorong.py` — 扩容表合并（备注列三种句式解析）
2. 运行脚本生成 `data/schools/*-catalog.json`，跑 `node test/test.js` 回归。
3. 提交推送。**改了课程库/图标/vendor 才需要升 sw.js 的 CACHE 版本**（页面与代码走网络优先，无需升版也能拿到最新）。

### 4. 接入新学校
- `data/schools.json` 加一条注册信息（含 `alias` 别名便于搜索）。
- 新建 `data/schools/<id>.json`，字段：
  - `name` / `short` / `campuses`（code+name，驱动校区徽标与天气定位）/ `campusGeo`（各校区经纬度）
  - `periods`（节次 → 时刻表）/ `semesterMonday`（第 1 周周一）/ `maxWeek`
  - `sections`（可选：单日视图上午/下午/晚上分段与时刻文案）
  - `catalogUrl`（可选，默认 `<id>-catalog.json`）
- 编写/复用转换脚本生成该课的课程库。
- sw.js `ASSETS` 与 dashboard 文案同步登记新校。

### 5. 数据库（Supabase）
- 首次部署按序执行 `sql/user_data_setup.sql`、`sql/forum_setup.sql`、`sql/forum_open_read.sql`；赞赏漏斗需 `support_opens` 表 + `support_stats()` RPC（幂等脚本已上线，见 dashboard 说明）。
- 统计 RPC：`stats_ping`（日活+回复红点+身份编号）、`stats_summary`、`stats_forum`、`support_stats`，均为 SECURITY DEFINER；前端只有 publishable key，权限全部由 RLS 保证。
- App 内所有云流量走双源反代：主线路 CF Worker（`api.courseshell.cloud`）→ 备胎 Netlify Functions，网络类错误自动切换（`withFailover`，每次调用失败最多切 1 次、每次会话累计最多切 2 次）。

### 6. 消息提醒（Web Push）
- 建表：首次部署执行 `sql/push_setup.sql`（订阅表 + 任务表 + 3 个 RPC；两张表对前端全锁，只能走 RPC）。
- VAPID 密钥：本地生成（`npx web-push generate-vapid-keys` 或 node 的 crypto），公钥填进 `app.js` 的 `VAPID_PUBLIC`，私钥只放 Netlify 环境变量。
- Netlify 环境变量（Site configuration → Environment variables）：`SUPABASE_SERVICE_KEY`（service_role）、`VAPID_PUBLIC_KEY`、`VAPID_PRIVATE_KEY`、`VAPID_SUBJECT`（如 `mailto:你的邮箱`）；设置后需重新部署一次生效。
- 投递链路：前端在本机算好未来 30 天任务（复用周次/微调逻辑）→ 经 RPC 写 `push_jobs`；`netlify/functions/push-sender.mjs` 是定时函数（cron `*/10 23,11,12 * * *` UTC = 北京时间 7/19/20 点窗口），用 web-push 投递，404/410 自动清理失效订阅。手动测试：Netlify UI → Functions → push-sender → Run now。
- 为什么不用 GitHub Actions：推送含 `.github/workflows/` 的文件需要 Token 的 `workflow` 权限；Netlify 定时函数所有套餐可用（免费额度内），无需该权限，也跟主站同一条部署链路。
- 注意：订阅按设备存（换设备重新开启）；任务到期超过 45 分钟自动作废，避免迟到推送；定时函数单次执行限 30 秒（当前量级足够）。

### 7. 缓存与发版（sw.js）
- 核心文件（HTML/JS/CSS）：**网络优先**，离线回落缓存——发版即时生效，无需升版。
- 课程库 `-catalog.json`：**缓存优先 + 后台自愈**（SWR）。
- vendor / 图标：缓存优先。
- 因此**只有**课程库 / 图标 / vendor 变化才把 `CACHE` 升版；升版后同步更新 dashboard「当前架构一览」里的 SW 版本文案。

### 8. 发版回退（一键）
- 发版前给当前提交打「已知可用」标签（本次已打）：
  - `v39` = 消息提醒上线前的稳定版
  - `v40-trial` = 消息提醒试用版
- 回退最稳一步：`git revert --no-edit v40-trial && git push`
  （生成反向提交、历史保留；Netlify / GitHub Pages 自动重新部署，核心文件网络优先，用户下次打开即回到旧版）
- 紧急硬回退（确认无人基于 main 开发时）：`git reset --hard v39 && git push --force-with-lease`
- 若回退涉及课程库 / 图标 / vendor（缓存优先资源），需再把 `sw.js` 的 `CACHE` 升一版，否则老用户仍拿缓存。

---

## 四、版本记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| v41 | 2026-09-12 | 感谢名单新增 1 位赞赏者（Ad infinitum · ¥1） |
| v40 | 2026-09-10 | **消息提醒（Web Push · 试用）**：早间课表（7:30）/ 晚间 DDL（20:00）/ 周日晚预览（19:00）三类推送，订阅与任务存 Supabase（表全锁 + RPC），Netlify 定时函数投递（含失效订阅清理）；「更多」菜单新增入口；测试 82→94 项 |
| v39 | 2026-09-09 | **系统性重构**：公共逻辑去重（节次时刻 `periodHM`、日期 `dateStrOf`/`todayStr`、天气码单表 `WMO_TABLE`、单按钮弹窗 `showInfoModal`、论坛课程下拉与附件上传抽取）；全部云端调用统一走双源反代（含论坛读写、附件、登录）；修复搜索建议 document 监听器泄漏；奶茶页计数改用本地时区日期；安装提示适配微信内文案；删除仓库垃圾文件与死 CSS；测试 67→82 项 |
| v38 | 2026-09-09 | 修复奶茶页计数：`supportPing` 改走 withFailover（此前直连被网络吞掉）；感谢名单新增 2 位赞赏者 |
| v37 | 2026-09-09 | 监测板 SW 版本文案更新；赞赏漏斗上线（support_opens + support_stats RPC，监测板新增「赞赏 funnel」卡片） |
| v36 | 2026-09-08 | 以最新教务导出重建校本部课程库（2120 门/3570 时段），补齐 `1800830751*` 培养单位课程；同天双时段句式解析修复 |
| v35 | 2026-09-08 | 奶茶页打开计数上线（匿名 did+day 去重，数据只进不出） |
| v34 | 2026-09-08 | 感谢名单上线（赞赏记录手工维护数组） |
| v33 | 2026-09-08 | 课程抽屉底部操作区重排：三等宽主操作 + 通栏危险操作 |
| v32 | 2026-09-08 | 「桌面快捷方式」指南：安卓长按直达 / iOS 快捷指令替代路径 |
| v31 | 2026-09-08 | 修复提醒弹窗 chip 选中态被 ghost 样式覆盖（CSS 特异性） |
| v30 | 2026-09-08 | 上课提醒弹窗改两段式：范围（仅下一节/全学期）+ 提前量（5/10/15/30 分钟） |
| v29 | 2026-09-08 | manifest shortcuts（今日课程/下节课深链）+ 下节课卡片 + 应用角标 + 未读回复红点驱动的论坛按钮 |
| v28 | 2026-09-08 | 天气卡展开态收起按钮移至标题行（摘要条隐藏） |
| v27 | 2026-09-08 | 上课提醒：.ics 系统日历导出（按周展开、VALARM 提前提醒、尊重个人微调） |
| v26 | 2026-09-07 | 国科大海洋学院（青岛基地）全链路接入：校配置、课程库（92 门）、校区徽标、天气定位 |
| v25 | 2026-09-06 | 校本部扩容三表合并（+20 门），备注列多句式解析；`merge_kuorong.py` |
| v24 | 2026-09-06 | 运行监测板（dashboard.html）：日活曲线、双源探测、承载水位、论坛生态 |

> 更早版本（v1–v23）为基线开发期：课表核心 / 笔记作业考试 / 云同步 / 论坛 / PWA / 多校框架 / 双源反代 / 统计与红点，此处不逐条展开。

---

## 五、常见问题（FAQ）

**Q：上课提醒是怎么实现的？会后台推送吗？**
A：两条通道：① **系统日历提醒**：导出 .ics → 写入系统日历 → 由系统按日程提醒（全平台可用）；② **消息提醒**（v40 起）：课壳直接推送每日课表与 DDL（iOS 16.4+ 装到主屏可用，国内部分安卓浏览器因推送服务受限收不到）。

**Q：为什么安卓长按有「下节课」，iPhone 没有？**
A：快捷方式（shortcuts）是 Android 桌面对 PWA 的能力，iOS 从未支持网页应用的快捷方式；iOS 用「快捷指令」App 达到同样效果。

**Q：奶茶页计数为什么之前一直是 0？**
A：v38 前该计数直连 Supabase（国内网络常被重置）且静默失败；v39 起与所有云调用一致走双源反代。

**Q：数据安全吗？**
A：私有数据（课表/笔记）按账号 RLS 隔离；论坛匿名可浏览、发言需登录、只能删自己的内容；作者身份时间与文件路径由数据库触发器保证，客户端伪造无效。

**Q：换手机数据怎么迁移？**
A：登录同一账号自动同步；或 `更多 → 备份与恢复` 导出 JSON 再导入。
