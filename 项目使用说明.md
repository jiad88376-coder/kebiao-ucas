# 课壳 · 项目使用说明

> 「课壳」— 国科大人自己的课表工具。粘贴课程代码，3 秒生成整学期课表。
>
> 本文档随版本迭代更新：每次发版时同步更新文末「版本记录」（`CACHE` 仅在课程库 / 图标 / vendor 变化时才升版，见 §7）。
>
> 文档版本：v53 · 更新日期：2026-09-15

## 在线入口

| 入口 | 地址 | 当前状态 |
|---|---|---|
| **当前唯一可用** | https://jiad88376-coder.github.io/kebiao-ucas/ | GitHub Pages，国内可访问 |
| 暂停中 | https://kebiao-ucas.netlify.app | Netlify 免费额度已用尽，站点被平台暂停，访客看到的是 "Site not available" 占位页；额度按周期重置后自动恢复 |
| 宣传域名 | https://kebiao.courseshell.cloud | 301 跳转到当前可用入口 |
| 运行监测 | https://jiad88376-coder.github.io/kebiao-ucas/dashboard.html | 日活 / 论坛 / 赞赏漏斗 |
| 广告后台 | https://jiad88376-coder.github.io/kebiao-ucas/admin.html | 见 §9（发布以 GitHub Pages 为准） |

**⚠️ Netlify 暂停期间的连带影响**（不只是静态站打不开）：
1. **消息提醒（Web Push）停止投递** —— 投递函数 `push-sender.mjs` 跑在 Netlify 定时任务上，站点暂停后 cron 不再触发。前端订阅状态仍显示「已开启」，属**静默失效**（详见 §6）。
2. **双源反代退化为单源** —— 备胎线路就是同一站点（`kebiao-ucas.netlify.app/.netlify/functions/supabase`），此时只剩 CF Worker 主线路（`api.courseshell.cloud`），`withFailover` 的容错能力归零。云同步 / 登录 / 论坛仍可用，但失去冗余。
3. 不受影响：主页 / dashboard / admin 三个静态页在 GitHub Pages 上照常工作；CF 反代主线路、课程库、PWA 离线缓存、.ics 日历导出均无关。

---

## 一、快速上手（用户视角）

### 1. 生成课表
- 粘贴课程代码：在选课系统里复制课程代码（如 `180081070200P1001H-1`），每行一个，点「生成课表」。
- 搜索添加：输入课程名称或代码，从建议列表点选。
- 支持批量：`更多 → 粘贴课程代码` 一次导入全班同学的代码（Ctrl+Enter 也能提交）。
- 时间冲突自动检测：精确到「周次 × 星期 × 节次」，补考周次不重叠不会误报；冲突的课块有红框和「冲突」角标。

### 2. 看课表
- 周切换：顶栏 ‹ › 翻周，点周标签回到本周，「全部周次」看整学期。
- 手机端默认聚焦今天（单日大视图），星期条一键切换；电脑端默认整周网格。
- 本周无课时自动顺延到最近有课的周，并提示。
- 单日视图：☀️上午 / 🌤下午 / 🌙晚上 三段卡片，置顶逐小时天气（按课程所在校区取坐标），其下是赞助栏（单条展示，点「详情」查看完整内容），空课时段有彩蛋文案。
- 课程色块按属性着色（核心/专业/公共/研讨/实验），底部有图例。

### 3. 课程详情（点课程块打开抽屉）
- 基本信息卡：教师 / 院系 / 学分 / 学时 / 考核方式 / 上课时间与教室。
- 笔记：可带图（本地压缩，最多 6 张），按日期归档。
- 作业：截止日倒计时徽章（剩 N 天 / 已逾期）。
- 考试：类型 / 日期 / 时间 / 地点，倒计时。
- 📍 调整信息：只改你自己课表里的显示——换教室、换老师、换上课时间（星期/节次），可选「整个学期」或「仅某一天」（调课临时安排）。会跟随云同步。
- 🔔 上课提醒：生成 .ics 日历文件 → 用系统「日历」打开 → 全学期课次连同提醒一次性写入，由系统按时提醒（iOS/安卓/电脑通用）。可选范围（仅下一节 / 全学期）和提前量（5/10/15/30 分钟）。
- 💬 讨论区：这门课的讨论与资料共享区。

### 4. 云同步与备份
- 未登录：一切数据只存在本机浏览器 localStorage。
- 登录（邮箱 + 密码，注册需邮箱验证码）：课表 / 笔记 / 作业 / 考试自动云同步；换设备登录同一账号即可互通。双端都有数据时按「修改时间新者胜」静默合并。
- `更多 → 立即云备份`：手动推送一次。
- `更多 → 备份与恢复`：导出/导入 JSON 备份文件（清缓存、换手机前的保险）。

### 5. 安装与桌面直达
- 安装成 App：`更多 → 安装成手机 App`（安卓浏览器菜单「安装应用」；iPhone 用 Safari 分享 → 添加到主屏幕）。安装后离线也能看课表。
- 桌面快捷方式（Android）：安装后**长按课壳图标** → 「今日课程」「下节课」直达。
- iPhone/iPad：iOS 不支持网页的快捷方式长按菜单，用「快捷指令」App 建「打开 URL」指向 `https://jiad88376-coder.github.io/kebiao-ucas/?view=next`，再添加到主屏幕即可（`更多 → 桌面快捷方式` 里有分步图文）。
- 应用角标：今天还剩几节课（Chromium 系支持，iOS 静默跳过）。

### 6. 分享与社区
- `分享`：推荐给同学，带你的编号（你是第 N 位课壳人）；支持 `?c=代码1,代码2` 链接一键导入。
- 自由论坛：发帖 / 回帖 / 📎上传资料（≤5MB）。未登录可浏览，发言需登录；只能删除自己的内容。
- 每门课有独立的「讨论与资料区」（课程抽屉 → 讨论区）。
- 感谢名单：页脚「🙏 感谢名单」；赞赏走页脚「☕ 请作者喝杯奶茶」。

### 7. 外观
- 🌗 主题按钮：跟随系统 → 强制深色 → 强制浅色 三档循环，系统主题变化时实时跟随。

### 8. 消息提醒（手机推送）
- `更多 → 消息提醒（手机推送）`：开启后由课壳直接推送——每天 7:30 今日课程（有课才发）、20:00 明日作业/考试截止、周日 19:00 下周课表预览、21:00 漂流瓶召回（有课表才发）；四类可分别开关。
- 与「系统日历提醒」相互独立：无需导入日历，内容随课表变化（含你的调课/换教室）；iOS 16.4+ 需先把课壳「添加到主屏幕」再从桌面打开；国内部分安卓浏览器收不到（依赖 Google 推送服务），可继续用日历提醒。
- 提醒任务只提前算 30 天，打开 App 会自动续算；换设备需重新开启。

### 9. 漂流瓶（匿名话题作答 + 随机捞瓶）
- **入口**（三处，都可进）：
  1. 顶栏第一个按钮「🫧 漂流瓶」
  2. **课表页顶部的「今日主题」栏**（在顶栏与「第 N 周」之间的那条）—— 把当天话题前置到首屏，点击直达；这是日常最高的入口
  3. `更多 → 漂流瓶`
  **不需要登录**——匿名参与，用设备号作身份。
- **玩法**：每天有 **3–5 个话题**（外加一个「不拘话题」）→ 选一个 → 匿名作答（纯文本 ≤100 字）→ 随机捞到别人写的瓶子（**跨全部话题**）→ 给喜欢的点「♥ 共鸣」。
- **账户等级与每日额度**（服务端权威，按北京时间零点重置）：

  | 等级 | 条件 | 每日可投 | 每日可捞 |
  |---|---|---|---|
  | 临时账户 | 未注册 | 3 | 3 |
  | 正式用户 | 已注册 | 5 | 5 |
  | 高级用户 | 已注册 且 连续登录 ≥3 天 | 10 | 10 |

  连续登录以「**打开 App**」为准（每天记一次）；间隔 2–3 天算宽限（连续天数不变），**超过 3 天未打开则连续天数清零、掉回正式用户**。
- **规则**：
  - 当日 24:00 后不能再投（服务端按北京时间判定）
  - 同一台设备不会捞到同一瓶，也捞不到自己投的
  - 共鸣免费，不消耗额度；同一瓶只能共鸣一次
  - 次日展示昨日「最共鸣 Top3」（只上前 3 名，每条带所属话题）
  - 池子捞空 / 额度用完会给彩蛋文案；临时账户额度用完会引导注册
- **话题库**：`data/topics.json`，作者人工维护，可随时补充（见 §10）。

---

## 二、兼容性说明

| 环境 | 情况 |
|---|---|
| iOS Safari | 可添加到主屏幕；无长按快捷方式、无应用角标；提醒走系统日历 |
| Android Chrome | 完整 PWA（安装、快捷方式、角标） |
| 微信 / QQ 内置 | 无法安装 PWA，顶部有引导条提示「右上角 ⋯ → 在浏览器打开」 |
| 离线 | 已缓存页面与课程库可离线查看；论坛 / 同步 / 天气需网络 |

---

## 三、维护者指南

### 1. 目录结构
```
schedule-app/
├── index.html / app.js / style.css / manifest.json / sw.js   # PWA 外壳与逻辑（无构建、原生 JS）
├── package.json                       # 唯一依赖 web-push（GitHub Actions 定时投递用）
├── dashboard.html                     # 运行监测板（独立页，直连统计 RPC）
├── admin.html                         # 广告后台（Token 门禁：编辑/传图/一键发布）
├── .github/workflows/push-sender.yml  # 消息提醒投递定时任务（cron）· ⚠️ 必须用 git 手工提交，API 推不了
├── .github/scripts/push-sender.mjs    # 投递逻辑（原 netlify/functions/push-sender.mjs 平移而来）
├── vendor/supabase.min.js             # supabase-js 本地化（离线可用）
├── data/schools.json                  # 学校注册表（id/名称/别名/默认校）
├── data/sponsors.json                 # 赞助广告数据（admin.html 后台维护）
├── data/topics.json                   # 漂流瓶话题库（人工手改，见 §10）
├── data/schools/<id>.json             # 校级配置（作息/校历/校区坐标/分段）
├── data/schools/<id>-catalog.json     # 课程库（脚本生成，勿手改）
├── netlify/functions/supabase.js      # Supabase 反代（备线路，含 /diag 诊断）· 当前未启用：已从 PROXY_SOURCES 摘除
├── functions/cloudflare/worker.js     # Supabase 反代（唯一线路，绑定 api.courseshell.cloud）
├── sql/                               # user_data / forum / push / drift / 支持计数 建表脚本
├── scripts/                           # 课程库生成脚本（Python + openpyxl）
├── test/test.js                       # 纯逻辑单元测试（node 直接跑）
├── support-qr.jpg                     # 「请作者喝杯奶茶」页收款码
└── icons/                             # PWA 图标
```

### 2. 本地预览 / 测试
```bash
cd schedule-app
python -m http.server 8080     # 打开 http://localhost:8080
node test/test.js              # 118 项纯逻辑单测（解析/冲突/周次/微调/ICS/下节课/提醒任务/漂流瓶/反代线路/工具函数）
```
app.js 尾部 `module.exports` 暴露可测函数；DOM 相关逻辑不进测试。

### 3. 更新课程库（每学期）
1. 把教务导出的 xlsx 放本地，修改对应脚本头部的 `SRC` 路径：
   - `scripts/rebuild_ucas_from_export.py` — 校本部：以最新导出**重建**课程库（新数据覆盖 + 保留现库独有课程）
   - `scripts/export_catalog_hias.py` — 杭高院
   - `scripts/export_catalog_qingdao.py` — 海洋学院（青岛）
   - `scripts/merge_kuorong.py` — 扩容表合并（备注列三种句式解析）
2. 运行脚本生成 `data/schools/*-catalog.json`，跑 `node test/test.js` 回归。
3. 提交推送。**改了课程库/图标/vendor 才需要升 sw.js 的 CACHE 版本**（页面与代码走网络优先，无需升版也能拿到最新）。

### 4. 接入新学校
- `data/schools.json` 加一条注册信息（含 `alias` 别名便于搜索）。
- 新建 `data/schools/<id>.json`，字段：
  - `name` / `short` / `campuses`（code+name，驱动校区徽标与天气定位）/ `campusGeo`（各校区经纬度）
  - `periods`（节次 → 时刻表）/ `semesterMonday`（第 1 周周一）/ `maxWeek`
  - `sections`（可选：单日视图上午/下午/晚上分段与时刻文案）
  - `catalogUrl`（可选，默认 `<id>-catalog.json`）
- 编写/复用转换脚本生成该课的课程库。
- sw.js `ASSETS` 与 dashboard 文案同步登记新校。

### 5. 数据库（Supabase）
- 首次部署按序执行 `sql/user_data_setup.sql`、`sql/forum_setup.sql`、`sql/forum_open_read.sql`；赞赏漏斗需 `support_opens` 表 + `support_stats()` RPC（幂等脚本已上线，见 dashboard 说明）。
- 统计 RPC：`stats_ping`（日活+回复红点+身份编号）、`stats_summary`、`stats_forum`、`support_stats`，均为 SECURITY DEFINER；前端只有 publishable key，权限全部由 RLS 保证。
- App 内所有云流量走 CF Worker 反代（`api.courseshell.cloud`），绕过 GFW 对 `*.supabase.co` 的 SNI 阻断。**当前只有这一条线路**：EdgeOne 备胎站点被平台回收下线；Netlify 备胎于 v50 摘除（站点被平台暂停，留着必然失败的备胎只会让故障路径多撞一次死站）。`withFailover` 轮换机制**保留未删**——将来接入第二个源时，往 `PROXY_SOURCES` 追加一条即自动恢复（每次调用失败最多切 1 次、每次会话累计最多切 2 次）。

### 6. 消息提醒（Web Push）
- 建表：首次部署执行 `sql/push_setup.sql`（订阅表 + 任务表 + 3 个 RPC；两张表对前端全锁，只能走 RPC）。
- VAPID 密钥：本地生成（`npx web-push generate-vapid-keys` 或 node 的 crypto），**公钥填进 `app.js` 的 `VAPID_PUBLIC`**，私钥只放 Secrets（前端绝不能有私钥）。
- **换密钥（轮换）时怎么做** —— 顺序不能错，否则用户会静默收不到推送：
  1. 改 `app.js` 的 `VAPID_PUBLIC` 为新公钥 → 提交部署（代码里已有 `vapidChanged()` 自愈逻辑，见下）
  2. 更新 Secrets 里的 `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY`
  3. 清空订阅表：`delete from public.push_subscriptions;`（旧行全部绑在旧公钥上，已无用）
  4. 各设备**打开一次 App** 即自动重订（`initPush` 检测到指纹变化 → 静默 unsubscribe + subscribe → `pushSyncSub` 回写新行）
  - 自愈原理：`app.js` 把公钥尾 12 位存进 `kebiao:vapidfp`。指纹不一致（含首跑的"没有指纹"）就丢弃旧订阅重订一次，用户无感。**没有这段逻辑的话，旧订阅不会自己消失**——推送服务会因 VAPID 签名不匹配拒收，表现是"提醒显示已开启但永远收不到"。
  - 重建订阅时会一并清掉 `PUSH_JOBS_AT_KEY` / `PUSH_JOBS_HASH_KEY`，否则新订阅可能因为"课表没变、6 小时内不重算"而拿不到任何提醒任务。
- **投递端跑在 GitHub Actions**（v51 起，原 Netlify 定时函数已删除）：
  - 脚本 `.github/scripts/push-sender.mjs`，工作流 `.github/workflows/push-sender.yml`
  - Secrets（Settings → Secrets and variables → Actions）：`SUPABASE_URL`（可省，脚本内有默认值）、`SUPABASE_SERVICE_KEY`（service_role）、`VAPID_PUBLIC_KEY`、`VAPID_PRIVATE_KEY`、`VAPID_SUBJECT`
  - cron `*/10 11,12,13,23 * * *`（UTC）= 北京时间 19/20/21 点 + 次日 7 点窗口；手动测试：Actions → push-sender → Run workflow
- 投递链路：前端在本机算好未来 30 天任务（复用周次/微调逻辑）→ 经 RPC 写 `push_jobs` → 定时任务扫到期任务用 web-push 投递，404/410 自动清理失效订阅、失败重试至 `attempts>=4` 作废。
- 四类任务（tag 前缀）：`m-` 早间课表 7:30 / `d-` 晚间 DDL 20:00 / `w-` 周日晚预览 19:00 / `f-` 漂流瓶召回 21:00。偏好存 `kebiao:pushprefs`，默认值由 `defaultPushPrefs()` 统一给出（旧设备缺新键时按默认开启）。
- 为什么不再用 Netlify：其免费额度是**账户级共享池**，用尽即暂停整站（含 Functions），投递会**静默失效**（前端订阅状态照旧显示"已开启"，毫无报错）。Actions 无此问题，且公开仓库额度对本项目量级绰绰有余。
- ⚠️ 两个 Actions 的注意点：① **workflow 文件必须用 git 手工提交** —— Contents API 拒绝通过 API 创建 `.github/workflows/*`（需 `workflow` 权限），`admin.html` 的经典 Token 没有该权限；② 定时任务可能延迟 15–30 分钟（排队），提醒类可接受。
- 注意：订阅按设备存（换设备重新开启）；任务到期超过 45 分钟自动作废（`STALE_MIN`），避免迟到推送；投递端只提前算 30 天，打开 App 会自动续算。

### 7. 缓存与发版（sw.js）
三类资源、三条策略：

| 资源 | 策略 | 效果 |
|---|---|---|
| 核心文件（HTML / `app.js` / `style.css` / `manifest.json` / `sponsors.json` / **`topics.json`**） | **协商缓存** `fetch(req, {cache:"no-cache"})` | 每次打开先发条件请求：文件没变则 CDN 返回 **304、正文 0 字节**，浏览器把本地副本交给页面；变了立刻拿到新内容。**发版即时生效、无需升版**，而且判断权在服务器——不存在"忘记改版本号导致改了不生效"的风险 |
| 课程库 `-catalog.json` | **缓存优先 + 后台自愈**（SWR） | 展示走缓存秒开，后台顺带更新（HTTP 缓存命中时不出网） |
| vendor / 图标 | 缓存优先 | 只下一次 |

- ⚠️ **`topics.json` 必须留在 `CORE` 里**（`app.js` 的 `CORE` 是裸文件名数组）。它既不以 `-catalog.json` 结尾、也不在 SWR 名单里，一旦从 `CORE` 移除就会落进"缓存优先"的兜底分支 —— **作者补充的新话题将永远取不到**，只有升 `CACHE` 清缓存才能看到。
- **只有**课程库 / 图标 / vendor 变化才把 `CACHE` 升版（`CACHE` 决定哪些资源进缓存）；核心文件与课程库的日常更新都**不需要**升版。升版后同步更新 dashboard「当前架构一览」里的 SW 版本文案。
- `install` 逐个补齐缺失资源（已缓存的不重复下载），所以改 `sw.js` 本身也不会触发一次约 1 MB 的整包重下；单个资源失败不再拖垮整次安装。
- ⚠️ **不要退回 `cache:"reload"`**。旧写法为"杜绝 10 分钟旧样式窗口"强制绕过 HTTP 缓存，代价是每次打开重下约 220 KB（gzip 后约 64 KB）。实测 GitHub Pages（Fastly）**不支持 `If-None-Match`（返回 200），但支持 `If-Modified-Since`（返回 304）**——协商缓存这条路才走得通。
- 实测传输量（gzip 后）：首次访问约 0.19 MB；复发访问约 0.003 MB（仅条件请求头）。

### 8. 发版回退（一键）
- 发版前给当前提交打「已知可用」标签（本次已打）：
  - `v39` = 消息提醒上线前的稳定版
  - `v40-trial` = 消息提醒试用版
- 回退最稳一步：`git revert --no-edit v40-trial && git push`
  （生成反向提交、历史保留；GitHub Pages 自动重新部署，核心文件走协商缓存，用户下次打开即回到旧版）
- 紧急硬回退（确认无人基于 main 开发时）：`git reset --hard v39 && git push --force-with-lease`
- 若回退涉及课程库 / 图标 / vendor（缓存优先资源），需再把 `sw.js` 的 `CACHE` 升一版，否则老用户仍拿缓存。

### 9. 赞助位维护（广告后台）
- 数据源：`data/sponsors.json`（启动时拉取；条目 `on:false` 或数组为空即整栏隐藏）。
- **推荐：网页后台** `admin.html` —— 入口：主站 `<主站地址>/admin.html` 或备用 `https://jiad88376-coder.github.io/kebiao-ucas/admin.html`（同一份文件；不同域名浏览器存储分开，Token 各自粘一次）。首次打开粘贴 GitHub **经典 Token**（`ghp_` 开头、勾 `repo` 权限，仅存本机浏览器）→ 网页里增删改广告、上传图片 → 「一键发布」：单个提交写入 main（Git Data API，新图自动用新文件名防缓存），**GitHub Pages 约 1 分钟生效**（Netlify 也会自动同步，若其部署异常不阻塞发布）。API 直连 api.github.com，国内一般可用，失败可开代理重试。
- 也可手工编辑 `data/sponsors.json` 提交。字段：`icon` / `title` / `tag` / `on`（缺省=启用）/ `detail`（`\n` 换行）/ `url`+`urlLabel` / `img`（路径或数组）。
- 主页单日视图在天气栏下方展示**一条**（按天轮换），点「详情 ▸」弹完整内容；多条时详情页底部可切换。
- 时效素材（如群二维码）过期：后台删旧图、传新图重新发布即可。

### 10. 漂流瓶维护
**话题库（最常做的维护）**
- 文件 `data/topics.json`，**每天 3–5 个话题**，按日期升序：
  ```json
  [{ "date": "2026-09-15", "topics": [
      { "key": "apple",  "text": "苹果上新，你焕了吗？", "tag": "生活" },
      { "key": "escape", "text": "今天最想逃的一节课是？", "tag": "课业" }
  ]}]
  ```
- `date` 必填；`topics` 数组里每条 `key`（同一期内唯一，用于瓶子归属）与 `text` 必填，`tag` 可选
- **改完提交即生效**，不用升 `CACHE`（它在 `sw.js` 的 `CORE` 里走协商缓存）
- 当天没排到 / 日期排完了 → 自动用内置兜底话题，功能不会白屏，**忘了补话题不会出事**
- 也兼容旧的「一天一条」格式 `{ date, text, tag }`（会被当成只有一个话题）

**数据库**
- 建表脚本两个，都在 Supabase SQL Editor 执行：`sql/drift_setup.sql`（基础）→ **`sql/drift_v2.sql`（v53 多话题 + 等级配额，幂等可重复跑）**
- 四张表，全部开 RLS 且不建策略 → 前端一个字段都读不到，只能走 RPC：
  - `drift_bottles` 投稿瓶：`topic_key` 标识所属话题（空串 = 不拘话题）；**v53 起同一天可投多次（按等级）**
  - `drift_fished` 捞取记录：`primary key(did, bottle_id)` 保证不重复捞，**是捞取额度的计数来源**
  - `drift_likes` 共鸣记录：**必须与 `drift_fished` 分开** —— 若点赞写进 `drift_fished`，「在墙上给昨天的瓶子点赞」会凭空吃掉今天的捞瓶额度，且那个瓶子会被当成"已捞"从池子里消失
  - `drift_members` 连续登录与等级：`member_key`（已注册按 `u:<uuid>`，匿名按 `d:<did>`）/ `streak` / `last_day`
- 5 个 RPC（`SECURITY DEFINER`，`grant execute ... to anon`）：`drift_checkin` / `drift_submit` / `drift_fish` / `drift_like` / `drift_status`；内部工具 `drift_mkey` / `drift_quota_of` / `drift_level` **不授予** anon
- **等级与配额公式（服务端权威，前端只展示）**：
  - 等级：未注册 = 0；已注册 = 1；已注册且 `streak ≥ 3` = 2
  - 配额：`level ≥ 2 → 10`，`level = 1 → 5`，`level = 0 → 3`（投、捞各自独立计）
  - 连续登录：当天已记不变 / 昨天记过 +1 / **间隔 2–3 天算宽限（不变）** / 超过 3 天重置为 1
- 时间口径：服务端一律用 `Asia/Shanghai` 判定"今天"，越界即封盘（客户端传日期参数，非当日直接报错）

**排查顺序（功能不工作时）**
1. `data/topics.json` 是否有今天的话题（没有会走兜底，属正常，不是故障）
2. Supabase 里 `drift_*` 四张表与 5 个 RPC 是否已建（**漏跑 `drift_v2.sql` 时会报 `push_jobs`/`drift_submit` 不存在或参数不匹配**）
3. 抓一次 `drift_submit` 的返回错误：`not today` = 日期口径不符（检查设备时区）、`throw quota used up` = 今日投瓶额度已用完
4. 捞不到瓶子是**正常**的：池子小 + 不捞自己的 → 人少时会频繁给彩蛋文案

---

## 四、版本记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| v53 | 2026-09-15 | **漂流瓶升级：每天多话题 + 账户等级配额**。① 话题库改为**每天 3–5 个话题**（`data/topics.json` 结构改为 `{date, topics:[{key,text,tag}]}`，兼容旧的一天一条），投瓶时选一个，也可选**「不拘话题」**投进大杂烩池；捞瓶默认跨全部话题。② 新增**账户等级**：临时（未注册）3 投/3 捞 · 正式（已注册）5/5 · 高级（连续登录 ≥3 天）10/10，按天重置；连续登录以「打开 App」为准，间隔 2–3 天算宽限、超 3 天清零。③ 新表 `drift_members` + 新 RPC `drift_checkin`（启动时签到维护连续天数）；`drift_submit/fish/status` 增加等级配额与 `topic_key`。⚠️ **所有新增参数都带 DEFAULT** ⇒ 旧前端不升级也能继续调用（会按临时账户算）。**等级配额取代了「答了才能捞」**——投与捞现在互相独立。需执行 `sql/drift_v2.sql`；测试 118→125 项 |
| v52 | 2026-09-15 | 功能更名：**「今日一漂」→「漂流瓶」**（顶栏按钮、更多菜单、页面标题、推送文案统一改名；内部标识符、localStorage 键与数据库表名保持不变，因此无数据迁移）；课表页顶部（顶栏与周次条之间）新增**「今日主题」栏** —— 把当天话题前置到首屏，点击直接进入漂流瓶（`.topic-bar`，含深色样式）；当天话题文案改为「苹果上新，你焕了吗？」 |
| v51 | 2026-09-15 | **新功能「今日一漂」（话题墙 × 漂流瓶）**：每天一个话题、匿名作答（用设备号，不需登录）、**答了才能捞**（答一题得 3 次捞瓶、每日上限 5 次）、匿名共鸣、次日昨日话题最共鸣 Top3 上墙；顶栏入口由「自由论坛」换成「今日一漂」，论坛挪进「更多」菜单（`renderReplyBadge` 的硬编码宿主随之改为「更多」按钮，并给 `.icon-btn` 补 `position:relative`，否则红点会飞出按钮）；新增 `data/topics.json` 话题库（纳入 sw.js `CORE` 走协商缓存，改完即时生效）与 `sql/drift_setup.sql`（3 表 + 4 个 RPC，匿名写一律走 SECURITY DEFINER）；推送新增第 4 类「今日一漂召回」（21:00，tag `f-`，有课表才发）；**消息提醒投递从 Netlify 定时函数迁到 GitHub Actions**（Netlify 额度用尽整站被暂停导致投递静默失效），`netlify/functions/push-sender.mjs` 已删除；**同时轮换 VAPID 密钥**（旧私钥只存在于已暂停的 Netlify 后台，改用新生成的一对）并新增 `vapidChanged()` 自愈逻辑 —— 密钥变更后设备自动重订、用户无感，否则旧订阅会因签名不匹配静默失效；测试 98→118 项 |
| v50 | 2026-09-15 | **省流量 · 去死链**：① 摘除 Netlify 备胎反代（其站点被平台暂停，留着只会让故障路径多撞一次死站；`withFailover` 机制保留，接第二个源时追加一条即恢复）；② 核心文件缓存策略由 `cache:"reload"` 改为 `no-cache` **协商缓存** —— 实测 GitHub Pages 支持 `If-Modified-Since` 返回 304（不支持 `If-None-Match`），文件未变时正文 0 字节，每次打开由约 64 KB 降至约 3 KB，且发版仍即时生效、无"忘记改版本号"风险；③ SW `install` 改为逐个补齐缺失资源 —— 改 `sw.js` 不再触发整包重下，单个资源失败也不再拖垮整次安装；④ dashboard 移除 Netlify 探测与水位卡，带宽水位口径由"1.5 MB/次"修正为实测值；测试 94→98 项（新增反代线路与 publishable key 不变量） |
| v49 | 2026-09-15 | 文档更正：**Netlify 不是部署故障，而是免费额度用尽导致整站被平台暂停**（更正 v48 的判断），当前唯一可用入口为 GitHub Pages；「在线入口」新增暂停期间的连带影响说明（消息提醒停止投递、双源反代退化为单源）；`sql/push_setup.sql` 注释更正为 Netlify 定时函数（原误写 GitHub Actions） |
| v48 | 2026-09-14 | 后台页补 `.hidden` 样式（未连接时不再露出编辑区/发布按钮）；文档更新：广告后台与发布链路以 **GitHub Pages 为准**（Netlify 静态部署异常，排查修复中，不阻塞广告管理） |
| v47 | 2026-09-13 | **广告后台（admin.html）**：网页编辑广告 / 上传图片 / 一键发布（GitHub Contents + Git Data API 单提交，Token 门禁仅存本机）；广告数据迁移到 `data/sponsors.json`（启动拉取，SW 网络优先缓存）；当前广告内容就位但停用（on:false），后台可自行开关 |
| v46 | 2026-09-13 | 赞助详情支持多图（`img` 兼容数组，纵向堆叠、点击放大）；烤骨头广告补充商家相册页 |
| v45 | 2026-09-13 | 赞助详情补充：进群二维码（`sponsors/kaogutou-group-qr-0912.jpg`，9月20日前有效，更新时换新文件名）+ 门店地址 |
| v44 | 2026-09-12 | 赞助栏上线首条广告（烤骨头火锅自助 · 课壳专属优惠） |
| v43 | 2026-09-12 | **赞助栏**：单日视图天气栏与上午段之间新增单条赞助位（按天轮换、可留空隐藏），「详情」弹窗展示完整广告（支持正文/图片/跳转/多条切换）；`SPONSORS` 数组手工维护，附使用说明 §9 |
| v42 | 2026-09-12 | **精简**：移除 EdgeOne 残留反代函数、已被 rebuild 脚本取代的 `export_catalog.py`、无用 DOM id（weekbar）；全部顶层声明与样式类经引用扫描确认在用，功能不变，测试 94 项通过 |
| v41 | 2026-09-12 | 感谢名单新增 1 位赞赏者（Ad infinitum · ¥1） |
| v40 | 2026-09-10 | **消息提醒（Web Push · 试用）**：早间课表（7:30）/ 晚间 DDL（20:00）/ 周日晚预览（19:00）三类推送，订阅与任务存 Supabase（表全锁 + RPC），Netlify 定时函数投递（含失效订阅清理）；「更多」菜单新增入口；测试 82→94 项 |
| v39 | 2026-09-09 | **系统性重构**：公共逻辑去重（节次时刻 `periodHM`、日期 `dateStrOf`/`todayStr`、天气码单表 `WMO_TABLE`、单按钮弹窗 `showInfoModal`、论坛课程下拉与附件上传抽取）；全部云端调用统一走双源反代（含论坛读写、附件、登录）；修复搜索建议 document 监听器泄漏；奶茶页计数改用本地时区日期；安装提示适配微信内文案；删除仓库垃圾文件与死 CSS；测试 67→82 项 |
| v38 | 2026-09-09 | 修复奶茶页计数：`supportPing` 改走 withFailover（此前直连被网络吞掉）；感谢名单新增 2 位赞赏者 |
| v37 | 2026-09-09 | 监测板 SW 版本文案更新；赞赏漏斗上线（support_opens + support_stats RPC，监测板新增「赞赏 funnel」卡片） |
| v36 | 2026-09-08 | 以最新教务导出重建校本部课程库（2120 门/3570 时段），补齐 `1800830751*` 培养单位课程；同天双时段句式解析修复 |
| v35 | 2026-09-08 | 奶茶页打开计数上线（匿名 did+day 去重，数据只进不出） |
| v34 | 2026-09-08 | 感谢名单上线（赞赏记录手工维护数组） |
| v33 | 2026-09-08 | 课程抽屉底部操作区重排：三等宽主操作 + 通栏危险操作 |
| v32 | 2026-09-08 | 「桌面快捷方式」指南：安卓长按直达 / iOS 快捷指令替代路径 |
| v31 | 2026-09-08 | 修复提醒弹窗 chip 选中态被 ghost 样式覆盖（CSS 特异性） |
| v30 | 2026-09-08 | 上课提醒弹窗改两段式：范围（仅下一节/全学期）+ 提前量（5/10/15/30 分钟） |
| v29 | 2026-09-08 | manifest shortcuts（今日课程/下节课深链）+ 下节课卡片 + 应用角标 + 未读回复红点驱动的论坛按钮 |
| v28 | 2026-09-08 | 天气卡展开态收起按钮移至标题行（摘要条隐藏） |
| v27 | 2026-09-08 | 上课提醒：.ics 系统日历导出（按周展开、VALARM 提前提醒、尊重个人微调） |
| v26 | 2026-09-07 | 国科大海洋学院（青岛基地）全链路接入：校配置、课程库（92 门）、校区徽标、天气定位 |
| v25 | 2026-09-06 | 校本部扩容三表合并（+20 门），备注列多句式解析；`merge_kuorong.py` |
| v24 | 2026-09-06 | 运行监测板（dashboard.html）：日活曲线、双源探测、承载水位、论坛生态 |

> 更早版本（v1–v23）为基线开发期：课表核心 / 笔记作业考试 / 云同步 / 论坛 / PWA / 多校框架 / 双源反代 / 统计与红点，此处不逐条展开。

---

## 五、常见问题（FAQ）

**Q：上课提醒是怎么实现的？会后台推送吗？**
A：两条通道：① **系统日历提醒**：导出 .ics → 写入系统日历 → 由系统按日程提醒（全平台可用）；② **消息提醒**（v40 起）：课壳直接推送每日课表与 DDL（iOS 16.4+ 装到主屏可用，国内部分安卓浏览器因推送服务受限收不到）。

**Q：为什么安卓长按有「下节课」，iPhone 没有？**
A：快捷方式（shortcuts）是 Android 桌面对 PWA 的能力，iOS 从未支持网页应用的快捷方式；iOS 用「快捷指令」App 达到同样效果。

**Q：奶茶页计数为什么之前一直是 0？**
A：v38 前该计数直连 Supabase（国内网络常被重置）且静默失败；v39 起与所有云调用一致走双源反代。

**Q：数据安全吗？**
A：私有数据（课表/笔记）按账号 RLS 隔离；论坛匿名可浏览、发言需登录、只能删自己的内容；作者身份时间与文件路径由数据库触发器保证，客户端伪造无效。

**Q：换手机数据怎么迁移？**
A：登录同一账号自动同步；或 `更多 → 备份与恢复` 导出 JSON 再导入。
